import * as userManager from "./manager";

/* TODO: Get them out of comments, but implement TypedRequest and create schemas first!!! */

// const updateUserDetails = async (
//   req: TypedRequest<typeof updateUserRequestSchema>,
//   res: Response
// ): Promise<IUser> => {
//   res.json(await UsersManager.updateUser(req.params.id, req.body));
// };

// const updateUserPassword = async (
//   req: TypedRequest<typeof updateUserRequestSchema>,
//   res: Response
// ): Promise<IUser> => {
//   res.json(await UsersManager.updateUser(req.params.id, req.body));
// };
